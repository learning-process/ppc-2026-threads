# Вычисление многомерных интегралов методом Симпсона

- **Student:** Редькина Алина Александровна, group **3823Б1ПР1**
- **Variant:** 11
- **Instructor:** Сысоев Александр Владимирович, доцент
- **Local reports:** [seq/report.md](seq/report.md), [omp/report.md](omp/report.md), [tbb/report.md](tbb/report.md),
  [stl/report.md](stl/report.md), [all/report.md](all/report.md)

## 1. Введение

Задача курса — реализовать численное интегрирование функции **d** переменных по прямоугольной области составной формулой
Симпсона и сравнить несколько моделей параллелизма (**SEQ**, **OpenMP**, **oneTBB**, **`std::async`**, **MPI** + локальная
многопоточность в **all**) на **одной и той же** постановке и **общих** тестах (**`tests/functional/main.cpp`**,
**`tests/performance/main.cpp`**).

## 2. Единая постановка задачи

**Вход:** границы **a**, **b**, вектор чётных **n_i**, функция **f**. **Выход:** скаляр — приближение интеграла.

**Корректность:** совпадение с последовательным эталоном (**SEQ**) на детерминированных тестах с допусками курса.

**Ограничения:** согласованность размерностей; **a_i < b_i**; **n_i** положительные чётные; **f** определена во всех узлах
сетки **∏(n_i+1)**.

## 3. Единая методика эксперимента

**Окружение (пример замеров автора):** CPU AMD Ryzen 7 7840HS; ОС Windows 11; **g++** 13.3.0; сборка **Release**.

**Переменные окружения курса:** **`PPC_NUM_THREADS`** (пробрасывается в **`OMP_NUM_THREADS`** для OpenMP); для MPI —
**`PPC_NUM_PROC`**; лимиты времени — **`PPC_TASK_MAX_TIME`**, **`PPC_PERF_MAX_TIME`** (см. документацию runner’а).

**Данные:** в функциональных тестах параметры задаются в коде; для производительности использовался сценарий курса с
**трёхмерной** функцией Гаусса и сеткой **100×100×100** (см. perf-тесты задачи).

**Метрики:**

- **Speedup** относительно **SEQ:** **S = T_seq / T_backend** (один и тот же размер задачи).
- **Efficiency** для потоковых backend-ов (ориентир): **E = S / T_раб**, где **T_раб** — число потоков OpenMP/TBB или число
  параллельных задач **`std::async`** на замере; для **ALL** отдельно фиксируются **`ranks`** и локальные потоки.

**Повторяемость:** несколько прогонов perf; в таблице ниже — представительное время (как в первоначальном отчёте); для
публикации предпочтительны **медиана** и дисперсия по серии.

**Прогрев / шум:** на ноутбуке без жёсткой фиксации governor и **taskset**; сравнения ниже относятся к указанной машине
и не переносятся автоматически на другие системы.

## 4. Сводка корректности

Все включённые в **`settings.json`** backend-ы (**seq**, **omp**, **tbb**, **stl**, **all**) проверяются общим набором из
**`tests/functional/main.cpp`** (1D/2D/3D, разные **f**). Результаты численно согласованы с **SEQ** в пределах тестов.
Производительность и корректность **не смешаны** в одной таблице: ниже только **perf**-срез; func-тесты — отдельный
проход.

## 5. Агрегированные результаты (производительность)

Один сценарий: **3D Гаусс**, сетка **100³** (см. локальные отчёты). Режим измерения — как в курсе (**`task_run`** / perf
runner). **T_раб** для потоковых версий в конкретном логе не зафиксирован; эффективность требует явного **PPC_NUM_THREADS**
при повторе.

| backend | mode (потоки / процессы) | size (узлы/параметр) | time, s | speedup **S** vs SEQ | notes |
| ------- | ------------------------ | --------------------- | ------- | -------------------- | ----- |
| seq | 1 | 3D **100³** | 0.108102 | 1.00 | baseline |
| omp | OpenMP | то же | 0.064491 | 1.68 | `schedule(static)`, `reduction` |
| tbb | `parallel_reduce` | то же | 0.066552 | 1.62 | `blocked_range` |
| stl | `std::async` | то же | 0.054318 | 1.99 | `hardware_concurrency()` |
| all | MPI + async | задать **P**, сравнить | — | — | внести после замеров **P** и **T** |

**Интерпретация:** на этой конфигурации **STL** дала наилучшее время; **OMP** и **TBB** близки. Разница не означает
«оптимальность» в общем случае — влияют планировщик ОС, кэш и фактическое **T**.

## 6. Интерпретация различий

- **SEQ** — опорная линия по времени и эталон значения.
- **OMP** — предсказуемое статическое разбиение **idx**; **reduction** убирает гонки.
- **TBB** — снижение ручного кода за счёт **parallel_reduce**; накладные расходы рантайма сопоставимы с OpenMP на данной
  задаче.
- **STL** — явное нарезание диапазонов и **async**; на замере ускорение выше, но оценка должна подкрепляться серией
  повторов.
- **ALL** — выигрыш ожидается при **P>1**; на одном процессе доминирует та же арифметика, плюс возможные накладные расходы
MPI.

## 7. Репродуцируемость

Команды в духе документации курса (из корня репозитория, пути уточнить по своему `CMake`):

```bash
git submodule update --init --recursive --depth=1
cmake -S . -B build -DUSE_FUNC_TESTS=ON -DUSE_PERF_TESTS=ON -DCMAKE_BUILD_TYPE=Release
cmake --build build --parallel
# Пример: потоки
export PPC_NUM_THREADS=4
python scripts/run_tests.py --running-type=threads --counts 1 2 4
# Пример: MPI / процессы
export PPC_NUM_PROC=2
python scripts/run_tests.py --running-type=processes --counts 2 4
# Производительность
python scripts/run_tests.py --running-type=performance
```

Точные цели бинарников и фильтры **gtest** — по актуальной структуре репозитория **`ppc-2026-threads`**.

## 8. Заключение

Поставленная цель — параллельные реализации одной квадратуры и сравнение backend-ов на общей методике — выполнена.
Наиболее быстрой на одном наборе замеров оказалась **STL**-версия; **OMP**/**TBB** дают сходное ускорение **~1.6×**.
Уточнение ускорения и эффективности требует фиксации **PPC_NUM_THREADS** и серии повторов.

Ограничения: результаты зависят от железа и фона ОС; **ALL** нужно оценивать отдельно при выбранных **P** и нагрузке сети.

## 9. Источники

1. Лекции Сысоева А. В. по курсу «Параллельное программирование».
2. Спецификация OpenMP, документация oneTBB (UXL Foundation), MPI Forum, `cppreference` (**std::async**), материалы курса
   **`ppc-2026-threads`**.

## 10. Приложение (краткие ссылки на код)

Подробные листинги и пояснения по каждому backend-у — в локальных **`report.md`**; корневой файл согласует только методику,
таблицы и выводы, без дублирования полного описания алгоритма.
