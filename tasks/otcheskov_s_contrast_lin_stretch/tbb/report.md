# Повышение контраста полутонового изображения посредством линейной растяжки гистограммы — TBB
- Student: Отческов Семён Андреевич
- Technology: TBB
- Variant: 28

## 1. Контекст
TBB (oneTBB) – это библиотека параллельного программирования на основе задач, которая автоматически управляет пулом потоков и балансировкой нагрузки.

Для задачи линейного растяжения гистограммы TBB предоставляет более высокий уровень абстракции по сравнению с OpenMP: декомпозиция на задачи, рекурсивное разбиение диапазонов, автоматическая настройка количества чанков. Наиболее интересно в данной задаче разбиение диапазонов и динамическое распределение задач.

## 2. Постановка задачи
Полная постановка задачи приведена в [`seq/report.md`](../seq/report.md#2-постановка-задачи).

- Вход: `std::vector<uint8_t>` – пиксели полутонового изображения.
- Выход: `std::vector<uint8_t>` той же длины – контрастированное изображение.
- Алгоритм:
  1. Найти `min` и `max` во входном векторе.
  2. Если `min == max` – скопировать вход в выход.
  3. Иначе для каждого пикселя `p`:
     - $output[i]=\frac{(input[i] - min_I) * (255 - 0)}{max_I-min_I}$
     - Привести к `[0,255]`.
- TBB-версия должна давать результат, идентичный SEQ (проверяется через инварианты `CheckRange`), и обеспечивать ускорение за счёт параллельной обработки.


## 3. Базовый алгоритм
Базовый алгоритм полностью совпадает с SEQ ([см. seq/report.md](../seq/report.md#3-базовый-алгоритм)).

1. Поиск глобальных `min` и `max` во входном векторе.
2. Если `min == max` – копирование входного вектора в выходной.
3. Иначе – преобразование каждого пикселя по формуле.

Оба этапа (`parallel_reduce` для поиска минимума/максимума, `parallel_for` для копирования/преобразования) являются итеративно-независимыми.

## 4. Схема распараллеливания
Какой примитив выбран: parallel_for / parallel_reduce / task_group.
Как определяется диапазон работы.
Какой grainsize используется и почему.
Какой partitioner используется.
Как ограничивалась конкуренция.

## 5. Детали реализации
Файлы: tbb/include/ops_tbb.hpp, tbb/src/ops_tbb.cpp
Локальные аккумуляторы, объединение результатов, работа с памятью.

## 6. Проверка корректности
Используются те же тестовые наборы, что и для SEQ (см. [seq/report.md](../seq/report.md#5-проверка-корректности)), с применением критерия `CheckRange`:
```cpp
bool CheckRange(const OutType &data) {
  if (data.empty()) return false;
  auto [min_it, max_it] = std::ranges::minmax_element(data);
  return (*min_it == 0 && *max_it == 255) || (*min_it == *max_it);
}
```
Дополнительные проверки:
- Ручная проверка для одного потока (`GetNumThreads() == 1`) – результат совпадает с SEQ.
- Для 2, 4, 6, 12, 24 потоков результаты (минимаксные значения) одинаковы, что подтверждает корректность редукции.

## 7. Экспериментальная среда
### 7.2. Тестовая инфраструктура
| Параметр   | Значение                                             |
| ---------- | ---------------------------------------------------- |
| CPU        | Intel Core i5 12400F (6 cores, 12 threads, 2500 MHz) |
| RAM        | 32 GB DDR4 (3200 MHz)                                |
| OS         | Windows 10 (10.0.19045)                              |
| Компилятор | MSVC 19.42.34435, Release Build                      |

### 7.2. Общие настройки
- Количество потоков задаётся через `ppc::util::GetNumThreads()`, которая читает переменную окружения `PPC_NUM_THREADS`.
- `task_arena` создаётся с этим числом потоков.

#### Размер задачи:
- Синтетическое изображение 10000 × 10000 (100 000 000 пикселей), генерируется программно со случайным контрастом.

#### Команда запуска:
```bash
set PPC_NUM_THREADS=4
./bin/ppc_perf_tests --gtest_filter="*Otcheskov*tbb*"
```
Аналогично для других значений потоков.


## 8. Результаты
| Режим | Потоков   | Время, s | Ускорение | Эффективность |
| ----- | --------- | -------- | --------- | ------------- |
| seq   | 1         | 1.667792 | 1.0000    | N/A           |
| tbb   | 2         | 1.039409 | 1.6046    | 80.2%         |
| tbb   | 3         | 0.731783 | 2.2791    | 76.0%         |
| tbb   | 4         | 0.585860 | 2.8467    | 71.2%         |
| tbb   | 6         | 0.454329 | 3.6709    | 61.2%         |
| tbb   | 12        | 0.375292 | 4.4439    | 37.0%         |
| tbb   | 24        | 0.384721 | 4.3351    | 18.1%         |



Комментарий о balancing и overhead.

## 9. Выводы
Когда TBB удобнее и когда overhead мешает.