# Выделение рёбер на изображении с использованием оператора Собеля — OMP

- **Студент**: Шакирова Есения Андреевна, 3823Б1ПР2
- **Технология**: OMP
- **Вариант**: Выделение рёбер на изображении с использованием оператора Собеля. Вариант 27.

## 1. Контекст

Исходная задача — применение оператора Собеля к полутоновому изображению для выделения границ
объектов. Последовательная версия служит эталоном корректности и базой для измерения ускорения
параллельных реализаций.

Оператор Собеля вычисляет градиент интенсивности изображения в каждой точке, применяя свёртку
с двумя ядрами 3×3: горизонтальным `Gx` и вертикальным `Gy`. Магнитуда градиента характеризует
резкость перехода интенсивности и используется как мера выраженности границы.

OpenMP предоставляет удобный механизм распараллеливания цикла по строкам изображения
с помощью директивы `#pragma omp parallel for`. По сравнению с последовательной версией
(SEQ) параллельная реализация использует те же ядра Собеля и ту же формулу магнитуды,
но внешний цикл распределяется между потоками автоматически.

## 2. Постановка задачи

**Входные данные**: контейнер `InType = ImgContainer`, содержащий:

- `width` — ширина изображения в пикселях;
- `height` — высота изображения в пикселях;
- `pixels` — вектор значений пикселей в диапазоне `[0, 255]`.

Цветные изображения форматов `.png`, `.jpg`, `.bmp` автоматически преобразуются
в полутоновые по формуле:

```bash
gray = 0.299 * R + 0.587 * G + 0.114 * B
```

Поддерживается текстовый формат `.txt`: первая строка содержит `width height`,
далее — значения пикселей.

**Выходные данные**: `OutType = std::vector<int>` — вектор магнитуд градиента в
диапазоне `[0, 255]`, размером `width * height`.

**Ограничения**: `width >= 1`, `height >= 1`, все значения пикселей принадлежат
`[0, 255]`, функция непрерывно определена для всей области изображения.

## 3. Базовый алгоритм

Алгоритм применяет оператор Собеля к каждому внутреннему пикселю изображения:

1. Для каждого пикселя `(row, col)`, где `1 <= row < height-1` и `1 <= col < width-1`,
   вычисляется свёртка с двумя ядрами `3×3`:

```bash
Gx = [[-1, 0, 1],      Gy = [[-1, -2, -1],
      [-2, 0, 2],             [ 0,  0,  0],
      [-1, 0, 1]]             [ 1,  2,  1]]
```

2. Вычисляется магнитуда градиента:

```bash
magnitude = sqrt(Gx^2 + Gy^2)
```

3. Результат ограничивается диапазоном `[0, 255]` и записывается в выходной вектор.

4. Граничные пиксели остаются нулевыми — для них невозможно применить полное ядро `3×3`.

Внешний цикл по строкам изображения распараллеливается с помощью OpenMP.
Каждый поток обрабатывает независимый набор строк и записывает результаты
в непересекающиеся ячейки выходного вектора, что исключает гонки данных.

**Временная сложность**: O(width × height / T), где T — число потоков.
**Память**: O(width × height).

## 4. Детали реализации

Реализация расположена в файлах `omp/include/ops_omp.hpp` и `omp/src/ops_omp.cpp`.
Класс `ShakirovaESobelEdgeDetectionOMP` наследуется от `BaseTask`.

`ValidationImpl` делегирует проверку входных данных методу `GetInput().IsValid()`.

`PreProcessingImpl` сохраняет ширину, высоту и вектор пикселей в поля класса,
инициализирует выходной вектор нулями нужного размера.

Основная логика распараллеливания реализована в `RunImpl`. Перед директивой OpenMP
размеры изображения копируются в локальные переменные `h` и `w`, которые передаются
в параллельный регион через `firstprivate`, исключая обращение к полям класса внутри
потоков. Ядра Собеля объявлены как `const std::array<std::array<int, 3>, 3>` и
переданы через `shared` — они доступны только на чтение, что безопасно.

`PostProcessingImpl` не выполняет дополнительных действий и возвращает `true`.

Эталонная реализация метода `RunImpl`:

```cpp
bool RunImpl() {
  const std::array<std::array<int, 3>, 3> k_gx = {{{-1, 0, 1}, {-2, 0, 2}, {-1, 0, 1}}};
  const std::array<std::array<int, 3>, 3> k_gy = {{{-1, -2, -1}, {0, 0, 0}, {1, 2, 1}}};

  auto &out = GetOutput();
  const int h = height_;
  const int w = width_;

#pragma omp parallel for default(none) shared(out, k_gx, k_gy) firstprivate(h, w) schedule(static)
  for (int row = 1; row < h - 1; ++row) {
    for (int col = 1; col < w - 1; ++col) {
      int gx = 0;
      int gy = 0;

      for (int ky = -1; ky <= 1; ++ky) {
        for (int kx = -1; kx <= 1; ++kx) {
          const int pixel = input_[((row + ky) * w) + (col + kx)];
          const auto ky_idx = static_cast<size_t>(ky) + 1;
          const auto kx_idx = static_cast<size_t>(kx) + 1;
          gx += pixel * k_gx.at(ky_idx).at(kx_idx);
          gy += pixel * k_gy.at(ky_idx).at(kx_idx);
        }
      }

      const int magnitude =
          static_cast<int>(std::sqrt(static_cast<double>((gx * gx) + (gy * gy))));
      out[(row * w) + col] = std::clamp(magnitude, 0, 255);
    }
  }

  return true;
}
```

Ключевые решения реализации:

- `default(none)` — все переменные внутри параллельного региона указаны явно,
  что исключает случайный захват лишних переменных.
- `firstprivate(h, w)` — каждый поток получает собственную копию размеров,
  избавляя от потенциальных обращений к полям класса.
- `schedule(static)` — строки распределяются между потоками равномерно блоками,
  что оптимально при равномерной вычислительной нагрузке на строку.

## 5. Проверка корректности

Корректность OMP-реализации проверялась на тех же тестах, что и SEQ-версия,
с обязательной проверкой совпадения выходных векторов. Тесты включают реальные
изображения форматов `.png` и текстовые матрицы `.txt` различных размеров.

| Тест           | Формат | Размер       | Описание                   | Результат |
| -------------- | ------ | ------------ | -------------------------- |---------- |
| test\_1.png    | PNG    | средний      | Полутоновое изображение    | PASSED    |
| test\_2.png    | PNG    | средний      | Полутоновое изображение    | PASSED    |
| test\_3.png    | PNG    | крупный      | Цветное - полутоновое      | PASSED    |
| test\_4.txt    | TXT    | малый        | Матрица из файла           | PASSED    |
| test\_5.txt    | TXT    | малый        | Матрица из файла           | PASSED    |
| test\_6.txt    | TXT    | малый        | Матрица из файла           | PASSED    |

Все 6 функциональных тестов проходят успешно при `n = 2`, `n = 4`, `n = 6`
MPI-процессов.

## 6. Экспериментальная среда

- **Процессор**: Intel Core i5-5200U @ 2.20GHz
- **ОС**: Zorin OS 16.3, ядро Linux 5.15.0-78-generic, `e-X555LJ`
- **Компилятор**: GCC 9.4.0 (сборка в режиме Release)
- **MPI**: Open MPI 4.0.3 с флагом `--oversubscribe`
- **Переменные окружения**: `PPC_NUM_THREADS=1`, `PPC_NUM_PROC=1`
- **Команды запуска**:

```bash
cmake --build build --config Release --parallel
mpirun --oversubscribe -n 4 ./ppc_func_tests \
    --gtest_filter="*shakirova_e_sobel_edge_detection_omp*"
mpirun --oversubscribe -n 4 ./ppc_perf_tests \
    --gtest_filter="*shakirova_e_sobel_edge_detection_omp*"
```

## 7. Результаты производительности

Производительность измерялась в режимах `pipeline` и `task_run` при `n = 2`,
`n = 4`, `n = 6` MPI-процессов. Для сравнения приведены результаты SEQ-версии
при `n = 2` (наилучшие значения предыдущего отчёта).

| Технология | Процессы | Режим    | Время (сек) |
| ---------- | -------- | -------- | ----------- |
| SEQ        | 2        | pipeline | 0.1058      |
| SEQ        | 2        | task_run | 0.0814      |
| OMP        | 2        | pipeline | 0.1138      |
| OMP        | 2        | task_run | 0.0862      |
| OMP        | 4        | pipeline | 0.2411      |
| OMP        | 4        | task_run | 0.1637      |
| OMP        | 6        | pipeline | 0.4178      |
| OMP        | 6        | task_run | 0.3231      |

При `n = 2` OMP-версия показывает время, сопоставимое с SEQ. Увеличение числа
MPI-процессов приводит к росту накладных расходов на синхронизацию и контекстные
переключения, что характерно для задач с высокой пропускной способностью памяти
и относительно малым объёмом вычислений на пиксель.

Ускорение OMP относительно SEQ (режим `task_run`, `n = 2`):

```bash
Speedup = T_seq / T_omp = 0.0814 / 0.0862 ≈ 0.94
```

Незначительное замедление при `n = 2` объясняется накладными расходами OpenMP
при малом числе потоков. При увеличении размера изображения ожидается более
выраженный прирост производительности.

## 8. Выводы

Реализованная OMP-версия оператора Собеля корректно выделяет рёбра на изображениях
произвольного размера и формата. Все 6 функциональных тестов проходят успешно при
различных конфигурациях запуска. Распараллеливание внешнего цикла по строкам с
помощью `#pragma omp parallel for` с явным управлением областями видимости переменных
(`default(none)`, `firstprivate`, `shared`) гарантирует корректность и отсутствие
гонок данных. Основным ограничением остаётся зависимость эффективности ускорения
от размера изображения: при малых изображениях накладные расходы OpenMP нивелируют
прирост от параллелизма.
