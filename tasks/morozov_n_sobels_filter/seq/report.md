# Выделение ребер на изображении с использованием оператора Собеля — SEQ

- Student: Морозов Никита Александрович
- Technology: SEQ
- Variant: 27

## 1. Контекст

В задачах связанных с обработкой изображений нередко требуется выделять конкретные объекты. Многие алгоритмы нахождения
объектов на изображении требуют выделенных границ объекта. Основной вопрос как же выделить сами границы. Данную задачу
решает оператор Собеля.

В данном отчете описывается последовательный алгоритм оператора (фильтра) Собеля.

Задачи обработки изображения - работа с большими массивами чисел, следовательно параллельная обработка поможет увеличить
эффективность вычислений. Последовательная версия - эталон корректности для будущих параллельных алгоритмов и точка
отсчета времени выполнения операции над изображением.

## 2. Постановка задачи

Требуется выделить ребра на изображении с использованием оператора Собеля.

Входные / выходные данные: изображение в оттенках серого с высотой `H` и шириной `W`.

В программе данные такого типа описаны структурой `Image`:

```cpp
struct Image {
  size_t height;
  size_t width;
  std::vector<uint8_t> pixels;
};
```

## 3. Базовый алгоритм

Пошаговое описание.

Алгоритм представляет собой проход по всем пикселям изображения (кроме крайних - граница в 1 пиксель) и вычисления для
них нового значения равного градиенту яркости изображения. Градиент вычисляется по следующей формуле
`sqrt(gradX^2 + gradY^2)`. Градиенты по осям `X` и `Y` получаются путем свертки ядра Собеля для конкретного направления.

```
Ядро для оси X:
-1   0   +1
-2   0   +2
-1   0   +1

Ядро для оси Y:
-1  -2  -1
 0   0   0
+1  +2  +1
```

Вычисленные значения свертки для пикселя записываются в новое изображение.

Операции свертки для каждого ядра = (9 операций на пиксель) _ 2 = 18; Свертка выполняется для каждого пикселя => `W _
H \* 18` операций занимает фильтрация изображения.

Асимптотика по времени: O(`H` \* `W`)

Так как размеры матриц свертки малы относительно изображения, то их размер можно не учитывать.

Асимптотика по памяти: O(`H` \* `W`)

Инварианты и критерий корректности:

1. Цвет пикселя находится в диапазоне от 0-255 до и после вычисления сверткиж
2. Результирующее значение пикселя `i,j` зависит от его окрестных (матрица 3х3) пикселей исходного изоражения.

## 4. Детали реализации

Для тестирования алгоритма и дальнейшего изучения его производительности был написан класс `MorozovNSobelsFilterSEQ`

`seq/include/ops_seq.hpp` содержит описание классса:

- Основные поля класса - это матрицы свертки и результирующее изображение.
- Методы для фильтрации изображения: `Filter`, `CalculateNewPixelColor`.

`seq/src/ops_seq.cpp`:

- ValidationImpl: проверяет, что созданное объект результирующего изображения одинаковый по размерам с входным
- PreProcessingImpl: -
- RunImpl: Запускает обработку изображения, сохраняет данные в `Output`.
- PostProcessing: -
- Filter: Проходит по всем пикселям входного изображения и рассчитывает новые значения для пикселей результата.
- CalculateNewPixelColor: Рассчитывает значение оператора Собеля для конкретного пикселя.

## 5. Проверка корректности

Проверка корректности алгоритма производилась следующим образом:

1. Были выбраны определенные паттерны рисунков на изображениях размером `3x3` `5x5` `7x7` и `9x9` пикселей.
1. Для данных изображений были заранее вычислены результаты алгоритма
1. Алгоритм получал тестовые паттерны как входные изображения
1. Производилось сравнение заранее посчитанного ответа и результата алгоритма

Все тесты были пройденны корректно.

Тестовые данные можно просмотреть в директории `data`.

## 6. Экспериментальная среда

### Среда #1

- CPU - Apple m2
- RAM - 16 GB
- OS - Docker Ubuntu 24.04 Host MacOs 15.7.3
- Compiler — g++ v13.3.0
- CMake build type - Realease

## 7. Результаты

Таблица с baseline-временем для p=1. Короткий комментарий о самом дорогом фрагменте.

## 8. Выводы

Почему именно эта версия используется как эталон для ускорения.
